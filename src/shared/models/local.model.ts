export interface Local {
    id: number;
    nombre: string;
    ubicacion: string;
    horariosAtencion: string;
    datosContacto: string;
    logoUrl?: string;
    // Otros campos según tu modelo de backend
  }

  